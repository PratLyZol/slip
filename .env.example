# Slip environment.
# With NONE of these set, the app runs fully in deterministic demo mode.

# Dynamic environment id (public). Get one at https://app.dynamic.xyz.
# When present, the real Dynamic wallet provider mounts and demo mode turns off
# (unless NEXT_PUBLIC_DEMO_MODE=true is also set).
NEXT_PUBLIC_DYNAMIC_ENV_ID=

# Unlink admin API key (SERVER-ONLY — never bundled to the client). Enables the
# real Unlink shielded path (deposit → private transfer → withdraw on Arc
# testnet). Without it, the privacy legs are simulated deterministically.
UNLINK_API_KEY=

# Unlink app id (public tenant label). Defaults to "slip" when unset.
NEXT_PUBLIC_UNLINK_APP_ID=slip

# Circle StableFX API key (SERVER-ONLY). Drives real USDC->EURC at claim time.
# Contact-a-rep credential (sales@circle.com). Real secrets go in .env, NOT here.
CIRCLE_STABLEFX_API_KEY=
# Optional StableFX REST base override (defaults to https://api.circle.com).
CIRCLE_STABLEFX_API_BASE=

# Funded Base Sepolia EOA private key (SERVER-ONLY) that pays the CCTP burn when
# aggregating funds onto Arc. Get Base Sepolia USDC + ETH from faucet.circle.com.
CCTP_PRIVATE_KEY=
# Optional Base Sepolia RPC override (defaults to https://sepolia.base.org).
BASE_SEPOLIA_RPC_URL=

# Force demo mode even when other keys are present (great for the live demo).
NEXT_PUBLIC_DEMO_MODE=true
