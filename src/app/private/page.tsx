import StubPage from "@/components/StubPage";

export default function PrivatePage() {
  return (
    <StubPage
      title="You can't read it"
      blurb="A one-tap view that links the on-chain transactions and shows the amount and the social graph are unreadable. Lands in a later phase."
    />
  );
}
